import { StyleSheet } from 'react-native';

module.exports = StyleSheet.create({
  removeButton: {
    flex: 1,
    backgroundColor: 'red',
    justifyContent: 'center',
    alignItems: 'center',
  },
  userIcon: {
    marginRight: 10,
  },
  swipeOutContainer: {
    borderWidth: 0,
  },
});
